export { Root } from './components'

[Home](/) | Python3 — _Latest Syntax — Updated: Sep 2020 — [✎ Edit page](https://github.com/ngduc/syntax.codes/edit/master/docs/python3.mdx) — [💬 Discuss](https://github.com/ngduc/syntax.codes/issues)_

<a className="tagButton" href="https://github.com/python/cpython/releases">3.9.0rc2</a> <a className="tagButton" href="https://github.com/python/cpython/tree/master/Misc/NEWS.d">CHANGELOG</a>

<img src="https://upload.wikimedia.org/wikipedia/commons/thumb/c/c3/Python-logo-notext.svg/120px-Python-logo-notext.svg.png" height="48" />

## Python 3

### Syntax

```.js
import json
import math
from dataclasses import dataclass, asdict
  
@dataclass
class Life:
  message: str = ''
  seed: float = round(math.pi, 2)  # 3.14
  answer: int = 0
  
  def getAnswerOfLife(self) -> int:
    arr = f'{self.seed}'.split('.')          # '3.14' -> ['3', '14']
    self.answer = int(arr[0]) * int(arr[1])  # 3 * 14 = 42
    return self.answer
  
  def shout(self) -> str:
    charArr = list(self.message)  # string to array of chars
    msg = ''
    for idx, item in enumerate(charArr):
      upperChar = chr(ord(charArr[idx]) - 32)
      msg = msg + (' ' if charArr[idx] == ' ' else upperChar)
    return f'{msg} : {asdict(self)}'
  
myLife = Life(message = 'hello world')
myLife.getAnswerOfLife()
print(myLife.shout())
```
#### References - External Links:
[Run this in Repl.it](https://repl.it/@ngduc/All-Syntax-Examples#main.py)

Hello world! Calculate for the ultimate answer of life http://b.link/ans42

1-page example of:
  - String, length, spliting, template string.
  - Characters, char codes.
  - Array, get array items, loop.
  - Number, casting, basic math calculation.
  - Function, return a value.
  - JSON, parsing.