export { Root } from './components'

[Home](/) | Stencil — _Latest Syntax — Updated: May 2020 — [Edit page](https://github.com/ngduc/syntax.codes/edit/master/docs/stencil.mdx)_

<a className="tagButton" href="https://github.com/ionic-team/stencil/releases">1.14.0</a> <a className="tagButton" href="https://github.com/ionic-team/stencil/blob/master/CHANGELOG.md">CHANGELOG</a>

<img src="https://i.ibb.co/4dtrd9Z/image.png" height="48" />

## Stencil 1.14

### Stencil Component

```.js
import { Component, Prop, State, h } from "@stencil/core";
  
export interface Item {
  title: string;
  done?: boolean;
}
@Component({
  tag: "my-component",
  styleUrl: "my-component.scss",
  shadow: true
})
export class MyComponent {
  @Prop() initialItems: Item[];
  @State() items: Item[] = [{ title: "Item 1" }];
  
  fetchMore = () => {
    this.items = [...this.items, { title: "Fetched item" }];
  };
  
  render() {
    return (
      <div>
        {this.items.length > 0 && (
          <ul>
            {this.items.map(item => (
              <li>{item.title}</li>
            ))}
          </ul>
        )}
        <button onClick={this.fetchMore}>Fetch More</button>
      </div>
    );
  }
}
```

#### References - External Links:
- [Run this in Codesandbox](https://codesandbox.io/s/stencil-example-l06tm?file=/src/components/my-component/my-component.tsx)
- [Stencil Docs](https://stenciljs.com/docs/state)
