export { Root } from './components'

[Home](/) | Stencil — _Latest Syntax — Updated: May 2020 — [✎ Edit page](https://github.com/ngduc/syntax.codes/edit/master/docs/stencil.mdx) — [💬 Discuss](https://github.com/ngduc/syntax.codes/issues/5)_

<a className="tagButton" href="https://github.com/ionic-team/stencil/releases">1.14.0</a> <a className="tagButton" href="https://github.com/ionic-team/stencil/blob/master/CHANGELOG.md">CHANGELOG</a>

<img src="https://i.ibb.co/4dtrd9Z/image.png" height="48" />

## Stencil 1.14

### Stencil Component

```.js
import { Component, Prop, State, h } from "@stencil/core";
  
export interface Item {
  title: string;
  done?: boolean;
}
@Component({
  tag: "my-component",
  styleUrl: "my-component.scss",
  shadow: true
})
export class MyComponent {
  @Prop() initialItems: Item[];
  @State() items: Item[] = [{ title: "Item 1" }];
  
  fetchMore = () => {
    this.items = [...this.items, { title: "Fetched item" }];
  };
  
  render() {
    return (
      <div>
        {this.items.length > 0 && (
          <ul>
            {this.items.map(item => (
              <li>{item.title}</li>
            ))}
          </ul>
        )}
        <button onClick={this.fetchMore}>Fetch More</button>
      </div>
    );
  }
}
```

<details>
  <summary>Expand to see the output.</summary>  
  <iframe
    src="https://codesandbox.io/embed/stencil-example-l06tm?module=/src/components/my-component/my-component.tsx&theme=light&view=preview&hidenavigation=1&hidedevtools=1"
    style={{ width: '100%', height: 300, border: 0, borderRadius: 4 }}
    allow="accelerometer; ambient-light-sensor; camera; encrypted-media; geolocation; gyroscope; hid; microphone; midi; payment; usb; vr; xr-spatial-tracking"
    sandbox="allow-autoplay allow-forms allow-modals allow-popups allow-presentation allow-same-origin allow-scripts"
  ></iframe>
</details>

### References - External Links:
- [Run this in Codesandbox](https://codesandbox.io/s/stencil-example-l06tm?module=/src/components/my-component/my-component.tsx) — [Stencil Docs](https://stenciljs.com/docs/state)
