export { Root } from './components'

[Home](/) | React — _Latest Syntax — Updated: Jun 2020 — [✎ Edit page](https://github.com/ngduc/syntax.codes/edit/master/docs/react.mdx) — [💬 Discuss](https://github.com/ngduc/syntax.codes/issues/1)_

<a className="tagButton" href="https://github.com/facebook/react/releases">16.13.1</a> <a className="tagButton" href="https://github.com/facebook/react/blob/master/CHANGELOG.md">CHANGELOG</a>

<img src="https://upload-icon.s3.us-east-2.amazonaws.com/uploads/icons/png/8575147831553750379-64.png" height="48" />

## React 16 in Typescript

### Functional Component (33 LOC)

```.tsx
import * as React from 'react';
  
export interface Item {
  id: string;
  title: string;
  done?: boolean;
}
export interface Props {
  initialItems?: Item[];
}
export default function TodoList({ initialItems = [] }: Props): React.ReactElement {
  const [items, setItems] = React.useState<Item[]>(initialItems);
  const [title, setTitle] = React.useState('');
  
  const addItem = () => {
    setItems([...items, { id: `${Math.random()}`, title }]);
    setTitle('');
  };
  const deleteItem = (index: number) => setItems(items.filter((_, idx) => idx !== index));
  return (
    <>
      {items.length > 0 && (
        <ul>
          {items.map((item, index) => (
            <li key={item.id}>
              {item.title} <button onClick={() => deleteItem(index)}>X</button>
            </li>
          ))}
        </ul>
      )}
      <input value={title} placeholder="Task Title" onChange={(e) => setTitle(e.target.value)} />
      <button onClick={addItem}>Add Item</button>
    </>
  );
}
```

<details>
  <summary>Expand to see the output.</summary>  
  <iframe
    src="https://codesandbox.io/embed/react-typescript-estfk?module=/src/TodoList.tsx&theme=light&view=preview&hidenavigation=1&hidedevtools=1"
    style={{ width: '100%', height: 300, border: 0, borderRadius: 4 }}
    allow="accelerometer; ambient-light-sensor; camera; encrypted-media; geolocation; gyroscope; hid; microphone; midi; payment; usb; vr; xr-spatial-tracking"
    sandbox="allow-autoplay allow-forms allow-modals allow-popups allow-presentation allow-same-origin allow-scripts"
  ></iframe>
</details>

#### References - External Links:
[Run this in Codesandbox](https://codesandbox.io/s/react-typescript-estfk?module=/src/TodoList.tsx) — [React Typescript Cheatsheet](https://github.com/typescript-cheatsheets/react-typescript-cheatsheet)

### Functional Component - Arrow syntax

```.tsx
const MyComponent: React.FC<Props> = ({ value }) => <div>Syntax</div>
```

### Class

```.tsx
interface Props {
  value: number
}
interface State {
  text: string
}
export default class MyComponent extends React.Component<Props, State> {
  static defaultProps: Props = {
    value: 0
  }
  state: State = { text: 'Example' }

  render() {
    return <div>Syntax {this.props.value} {this.state.text}</div>
  }
}
```

#

----

## React in Javascript

### Functional Component

```.tsx
function MyComponent(props) {
  const [text, setText] = React.useState('Example');

  return <div>Syntax {props.value} {text}</div> 
}
  
// arrow syntax
const AnotherComponent = (props) => <div>Syntax {props.value}</div>
```

### Class

```.tsx
export default class MyComponent extends React.Component {
  static defaultProps = {
    value: 0
  }
  state = { text: 'Example' }

  render() {
    return <div>Syntax {this.props.value} {this.state.text}</div>
  }
}
```
