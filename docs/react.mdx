export { Root } from './components'

[Home](/) | React — _Latest Syntax — Updated: May 2020 — [✎ Edit page](https://github.com/ngduc/syntax.codes/edit/master/docs/react.mdx) — [💬 Discuss](https://github.com/ngduc/syntax.codes/issues/1)_

<a className="tagButton" href="https://github.com/facebook/react/releases">16.13.1</a> <a className="tagButton" href="https://github.com/facebook/react/blob/master/CHANGELOG.md">CHANGELOG</a>

<img src="https://upload-icon.s3.us-east-2.amazonaws.com/uploads/icons/png/8575147831553750379-64.png" height="48" />

## React 16 in Typescript

### Functional Component

```.tsx
import * as React from 'react'
  
export interface Item {
  title: string
  done?: boolean
}
export interface Props {
  initialItems?: Item[]
} 
export default function TodoList({ initialItems = [] }: Props): React.ReactElement {
  const [items, setItems] = React.useState<Item[]>(initialItems)

  const fetchMore = () => {
    setItems([...items, { title: 'Fetched item' }])
  }
  return (
    <>
      {items.length > 0 && (
        <ul>
          {items.map((item) => (
            <li>{item.title}</li>
          ))}
        </ul>
      )}
      <button onClick={fetchMore}>Fetch More</button>
    </>
  )
}
```

### Functional Component - Arrow syntax

```.tsx
const MyComponent: React.FC<Props> = ({ value }) => <div>Syntax</div>
```

### Class

```.tsx
interface Props {
  value: number
}
interface State {
  text: string
}
  
class MyComponent extends React.Component<Props, State> {
  static defaultProps: Props = {
    value: 0
  }
  state: State = { text: 'Example' }

  render() {
    return <div>Syntax {this.props.value} {this.state.text}</div>
  }
}
```

#### References - External Links:
- [Run this in Codesandbox](https://codesandbox.io/s/react-typescript-estfk?file=/src/TodoList.tsx)
- [React Typescript Cheatsheet](https://github.com/typescript-cheatsheets/react-typescript-cheatsheet)
- [Typescript React Starter](https://github.com/microsoft/TypeScript-React-Starter)

#
---------------

## React in Javascript

### Functional Component

```.tsx
function MyComponent(props) {
  const [text, setText] = React.useState('Example');

  return <div>Syntax {props.value} {text}</div> 
}
  
// arrow syntax
const AnotherComponent = (props) => <div>Syntax {props.value}</div>
```

### Class

```.tsx
class MyComponent extends React.Component {
  static defaultProps = {
    value: 0
  }
  state = { text: 'Example' }

  render() {
    return <div>Syntax {this.props.value} {this.state.text}</div>
  }
}
```
