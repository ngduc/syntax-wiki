export { Root } from './components'

[Home](/) | React - _Latest Syntax - Updated: May 2020 at [Github](https://github.com/ngduc/syntax.codes)_

_React - Mar 19, 2020_ <a class="tagButton" href="https://github.com/facebook/react/releases">16.13.1</a> <a class="tagButton" href="https://github.com/facebook/react/blob/master/CHANGELOG.md">CHANGELOG</a>

<img src="https://upload.wikimedia.org/wikipedia/commons/thumb/a/a7/React-icon.svg/1024px-React-icon.svg.png" height="48" />

## React 16 in Typescript

### Functional Component

```.jsx
export interface Props {
  defaultCount: number
}
export default function MyCounter({ defaultCount = 0 }: Props): React.ReactElement {
  const [count, setCount] = React.useState<number>(defaultCount);

  const onClick = () => {
    setCount(count + 1);
  }
  return <button onClick={onClick}>Example - count: {count}</button>
}
```

### Functional Component - Arrow syntax

```.jsx
const MyComponent: React.FC<Props> = ({ value }) => <div>Syntax</div>
```

### Class

```.jsx
interface Props {
  value: number
}
interface State {
  text: string
}
class MyComponent extends React.Component<Props, State> {
  static defaultProps: Props = {
    value: 0
  }
  state: State = { text: 'Example' }

  render() {
    return <div>Syntax {this.props.value} {this.state.text}</div>
  }
}
```

#### References:
- [Live Codesandbox](https://codesandbox.io/s/react-typescript-estfk?file=/src/MyCounter.tsx)
- [React Typescript Cheatsheet](https://github.com/typescript-cheatsheets/react-typescript-cheatsheet)
- [Typescript React Starter](https://github.com/microsoft/TypeScript-React-Starter)

#
---------------

## React in Javascript

### Functional Component

```.jsx
function MyComponent(props) {
  const [text, setText] = React.useState('Example');

  return <div>Syntax {props.value} {text}</div> 
}

// arrow syntax
const AnotherComponent = (props) => <div>Syntax {props.value}</div>
```

### Class

```.jsx
class MyComponent extends React.Component {
  static defaultProps = {
    value: 0
  }
  state = { text: 'Example' }

  render() {
    return <div>Syntax {this.props.value} {this.state.text}</div>
  }
}
```
