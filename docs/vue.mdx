export { Root } from './components'

[Home](/) | Vue — _Latest Syntax — Updated: Jun 2020 — [✎ Edit page](https://github.com/ngduc/syntax.codes/edit/master/docs/vue.mdx) — [💬 Discuss](https://github.com/ngduc/syntax.codes/issues/3)_

<a className="tagButton" href="https://github.com/vuejs/vue-next/releases">3.0.0-beta.13</a> <a className="tagButton" href="https://github.com/vuejs/vue-next/blob/master/CHANGELOG.md">CHANGELOG</a>

<img src="https://upload.wikimedia.org/wikipedia/commons/thumb/9/95/Vue.js_Logo_2.svg/2560px-Vue.js_Logo_2.svg.png" height="48" />

## Vue 3 beta

### Component with Composition API (34 LOC)

```.js
<template>
  <v-fragment>
    <ul v-if="items.length > 0">
      <li v-for="(item, index) in items" :key="index">
        {{ item.title }}
        <a v-on:click="deleteItem(index)">X</a>
      </li>
    </ul>
    <input v-model="title" placeholder="Task Title">
    <button v-on:click="addItem">Add Item</button>
  </v-fragment>
</template>
  
<script>
import { ref, computed, onMounted } from 'vue';
  
export default {
  setup(props) {
    onMounted(() => console.log('component mounted!'));
    const items = ref([{ title: 'Item 1' }]);
    const title = ref('');
    const addItem = () => {
      items.value = [...items.value, { title: title.value }];
      title.value = '';
    };
    const deleteItem = (index) => items.value = items.value.filter((_, idx) => idx !== index);
    return {
      items,
      title,
      addItem,
      deleteItem
    };
  }
};
</script>
  
<style scoped></style>
```

<details>
  <summary>Expand to see the output.</summary>  
  <iframe
    src="https://codesandbox.io/embed/vue-3-8qy7f?module=/src/App.vue&theme=light&view=preview&hidenavigation=1&hidedevtools=1"
    style={{ width: '100%', height: 300, border: 0, borderRadius: 4 }}
    allow="accelerometer; ambient-light-sensor; camera; encrypted-media; geolocation; gyroscope; hid; microphone; midi; payment; usb; vr; xr-spatial-tracking"
    sandbox="allow-autoplay allow-forms allow-modals allow-popups allow-presentation allow-same-origin allow-scripts"
  ></iframe>
</details>

### References - External Links:
- [Run this in Codesandbox](https://codesandbox.io/s/vue-3-8qy7f?module=/src/App.vue) — [VueJS API](https://composition-api.vuejs.org/api.html) — [Vue 3 Composition API Cheatsheet](https://www.vuemastery.com/pdf/Vue-3-Cheat-Sheet.pdf)
