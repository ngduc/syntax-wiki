export { Root } from './components'

[Home](/) | Angular — _Latest Syntax — Updated: Jun 2020 — [✎ Edit page](https://github.com/ngduc/syntax.codes/edit/master/docs/angular.mdx) — [💬 Discuss](https://github.com/ngduc/syntax.codes/issues/2)_

<a className="tagButton" href="https://github.com/angular/angular/releases">9.1.7</a> <a className="tagButton" href="https://github.com/angular/angular/blob/master/CHANGELOG.md">CHANGELOG</a>

<img src="https://upload.wikimedia.org/wikipedia/commons/thumb/c/cf/Angular_full_color_logo.svg/80px-Angular_full_color_logo.svg.png" height="48" />

## Angular 9

### Component with Typescript (40 LOC)

```.js
import { Component, OnInit, OnDestroy, Input } from '@angular/core';
  
interface Item {
  title: string;
  done?: boolean;
}
@Component({
  selector: 'app-root',
  template: `
    <ul *ngIf="items.length > 0">
      <li *ngFor="let item of items; index as idx; trackBy: trackByFn">
        {{ item.title }}
        <a (click)="deleteItem(idx)">X</a>
      </li>
    </ul>
    <input value="{{ title }}" #titleInput (keyup)="onKey(titleInput.value)" />
    <button (click)="addItem()">Fetch More</button>
  `,
  // templateUrl: './app.component.html', // for template file
  styleUrls: ['./app.component.css']
})
export class AppComponent implements OnInit, OnDestroy {
  @Input(100) maxItems: number;
  ngOnInit(): void {}
  ngOnDestroy(): void {}
  
  title = '';
  items: Item[] = [{ title: 'Item 1' }];
  trackByFn(index: number, item: Item) {
    return index; // or: item.id
  }
  
  onKey(value: string) {
    this.title = value;
  }
  addItem(): void {
    this.items = [...this.items, { title: this.title }];
    this.title = '';
  }
  deleteItem(idx: number): void {
    this.items = this.items.filter((_, itemIdx) => itemIdx !== idx);
  }
}
```

#### References - External Links:
- [Run this in Codesandbox](https://codesandbox.io/s/angular-9-example-cr1nd?file=/src/app/app.component.ts)
- [Livecycle Hooks APIs](https://angular.io/guide/lifecycle-hooks)
